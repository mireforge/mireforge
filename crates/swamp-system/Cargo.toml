[package]
name = "swamp-system"
version = "0.0.9"
edition = "2021"
repository = "https://github.com/swamp/swamp"
license = "MIT"

[dependencies]
swamp-system-state = {path = "../swamp-system-state", version = "0.0.9"}
