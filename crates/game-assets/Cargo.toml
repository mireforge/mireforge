[package]
name = "swamp-game-assets"
version = "0.0.15"
edition = "2021"
repository = "https://github.com/swamp/swamp"
license = "MIT"
categories = ["game-development"]
description = "Game asset loading"

[dependencies]
int_math = "0.0.2"
monotonic-time-rs = "0.0.9"

swamp-font = { path = "../font", version = "0.0.15" }
swamp-render-wgpu = { path = "../render-wgpu", version = "0.0.15" }

# Limnus
limnus-assets = "0.0.15"
limnus-asset-registry = "0.0.15"
limnus-resource = "0.0.15"
limnus-asset-id = "0.0.15"
limnus-audio-mixer = "0.0.15"
