[package]
name = "swamp-boot-advanced-game"
version = "0.0.16-alpha.1"
edition = "2021"
license = "MIT"
description = "boot up an advanced game (logic, render, audio)"
repository = "https://github.com/swamp/swamp"
categories = ["game-development"]

[dependencies]
swamp-advanced-game = { path = "../advanced-game", version = "0.0.16-alpha.1" }
int_math = "0.0.2"
swamp-material = { path = "../material", version = "0.0.16-alpha.1" }
swamp-render-wgpu = { path = "../render-wgpu", version = "0.0.16-alpha.1" }
swamp-font = { path = "../font", version = "0.0.16-alpha.1" }

# Limnus
# limnus-app = "0.0.16"
# limnus-screen = "0.0.16"

limnus = "0.0.17-alpha.1"# TODO: Move DefaultPlugins
