[package]
name = "swamp-examples"
version = "0.0.10"
edition = "2021"
repository = "https://github.com/swamp/swamp"
license = "MIT"
categories = ["game-development"]

[dependencies]
tracing = "0.1.40"
swamp = { path = "../swamp", version = "0.0.10" }
