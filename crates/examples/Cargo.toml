[package]
name = "swamp-examples"
version = "0.0.9"
edition = "2021"
repository = "https://github.com/swamp/swamp"
license = "MIT"

[dependencies]
tracing = "0.1.40"

swamp-boot-game = { path = "../swamp-boot-game", version = "0.0.9"}
